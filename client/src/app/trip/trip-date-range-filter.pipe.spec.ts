import { TripDateRangeFilterPipe } from './trip-date-range-filter.pipe';

describe('TripDateRangeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TripDateRangeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
