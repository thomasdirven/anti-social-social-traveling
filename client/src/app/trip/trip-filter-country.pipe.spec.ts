import { TripFilterCountryPipe } from './trip-filter-country.pipe';

describe('TripFilterCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
