import { TripFilterDateRangePipe } from './trip-filter-date-range.pipe';

describe('TripFilterDateRangePipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterDateRangePipe();
    expect(pipe).toBeTruthy();
  });
});
