import { TripFilterCityPipe } from './trip-filter-city.pipe';

describe('TripFilterCityPipe', () => {
  it('create an instance', () => {
    const pipe = new TripFilterCityPipe();
    expect(pipe).toBeTruthy();
  });
});
